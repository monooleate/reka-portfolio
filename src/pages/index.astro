---
export const prerender = false;

const langHeader = Astro.request.headers.get("accept-language") || "";
const lang = langHeader.toLowerCase().startsWith("hu") ? "hu" : "en";

// Server-side redirect (HTTP 302)
return Astro.redirect(`/${lang}/`);
---